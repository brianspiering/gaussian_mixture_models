# Create environment:
# $ conda env create --force
name: gmm
channels:
  - conda-forge
dependencies:
  - python = 3.6
  - ipython
  - jupyter
  - scipy
  - numpy
  - scikit-learn
  - pandas
  - seaborn
    
# Start environment:
# $ source activate gmm